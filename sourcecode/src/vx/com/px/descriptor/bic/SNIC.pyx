

cimport numpy as np

from libc.stdlib cimport calloc, free
from libc.stdlib cimport malloc, free
from libc.stdio cimport *




#from libc.math cimport pow

import time
import numpy
import math
#cimport numpy

def LSNIC(  unsigned char[:,:] img,
            unsigned char[:,:] mask,
            unsigned char[:,:] roi,
            int side ):

    riofile.obj
    IMGW 200 
    IMGH 200 
    NUMBERLABELS 112
    r c1 c2 LABEL
    r c1 c2 LABEL
    r c1 c2 LABEL
    r c1 c2 LABEL
    r c1 c2 LABEL
    r c1 c2 LABEL
    r c1 c2 LABEL
    r c1 c2 LABEL
    r c1 c2 LABEL
    r c1 c2 LABEL
    r c1 c2 LABEL


    roi = [ (x, y), (x, y), (x, y) ]

